破坏单例的方式：
一：反射破坏单例
在恶汉式单例和内部静态类单例中可以通过在构造方法中增加对象的非空判断来避免单例被破坏，懒汉式则无法通过该方式避免，可以通过加一个全局变量来进行是否初始化了的依据
二：序列化破坏单例
正常情况下可以通过反序列化来进行创建对象，但在单例的时候则会出现多个对象，破坏单例，此时解决方法是在单例对象中增加readResolve 方法即可，
三：深度克隆破坏单例
解决方法，不实现cloneable接口，或者clone方法中直接返回单例对象